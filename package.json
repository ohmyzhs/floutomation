{
  "name": "flow-batch-studio",
  "version": "0.7.2",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check background.js && node --check content.js && node --check sidepanel.js && node --check offscreen.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.2"
  }
}
